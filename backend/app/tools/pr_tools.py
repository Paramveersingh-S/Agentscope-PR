# pr_tools.py
# parse_pr_diff, extract_file_types

def parse_pr_diff(diff_text: str) -> dict:
    """Parse unified diff format into files."""
    return {}
    
def extract_file_types(files: list) -> list:
    """Extract extensions from files."""
    return []
